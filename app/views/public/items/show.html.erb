<div class="container px-5 py-5 ps-sm-4">
  <div class="row">
    <div class="col-md-5 mx-auto">
     <label for="inputImage"></label>
      <%= attachment_image_tag @item, :image, :fill, 200, 200, fallback: "no_image.jpeg" %>
    </div>
    <div class="col-md-7">
     <h2>
      <strong><%= @item.name %></strong>
     </h2>

     <h5>
      <%= @item.introduction %>
     </h5>

     <h5>
       ¥<%= @item.with_tax_price %>(税込)
     </h5>

     <%= form_with model: @cart_item, url: cart_items_path, metod: :post, local: true do |f| %>
      <%= f.hidden_field :item_id, value: @item.id %>
      <div class="form-group row">
      <h5>
       <%= f.select :amount, [1, 2, 3, 4, 5, 6, 7, 8, 8, 9, 10], {include_blank: '個数選択'} %>
      </h5>

      <div>
       <%= f.submit "カートに入れる",class:  "btn btn-success btn-sm" %>
      </div>
      </div>
     <% end %>
    </div>
  </div>
</div>